---
- name: Unregister one empty service VM contract
  hosts: hypervisor
  become: true
  environment:
    LC_ALL: C
  vars:
    service_registry_operation: unregister
  roles:
    - role: brick_guard
      vars:
        brick_guard_brick: service_registry
    - service_registry
