---
- name: Start one managed standard HyperLab VM
  hosts: hypervisor
  become: true
  environment:
    LC_ALL: C
  vars:
    guest_operation: start
  roles:
    - role: brick_guard
      vars:
        brick_guard_brick: guest
    - guest
