---
- name: Create one verified offline service backup
  hosts: hypervisor
  become: true
  environment:
    LC_ALL: C
  vars:
    service_registry_operation: backup
  roles:
    - role: brick_guard
      vars:
        brick_guard_brick: service_registry
    - service_registry
