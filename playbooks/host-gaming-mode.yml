---
- name: Install the measured transactional host Gaming Mode
  hosts: hypervisor
  become: true
  roles:
    - role: brick_guard
      vars:
        brick_guard_brick: host_gaming_mode
    - host_gaming_mode
