---
- name: Reset one managed standard Hyperlab VM
  hosts: hypervisor
  become: true
  environment:
    LC_ALL: C
  vars:
    guest_operation: reset
  roles:
    - role: brick_guard
      vars:
        brick_guard_brick: guest
    - guest
