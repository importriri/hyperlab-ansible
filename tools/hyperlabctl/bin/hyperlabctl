#!/usr/bin/env python
"""Runs straight from a checkout: no install step, no PYTHONPATH."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from hyperlabctl.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
