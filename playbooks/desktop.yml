---
# Targeted cockpit reconciliation. lab.yml mounts it on the hypervisor; this
# playbook also serves workstation guests without coupling their availability
# to the host build.
- name: Mount the desktop cockpit
  hosts: hypervisor:workstations
  become: true
  roles:
    - desktop
